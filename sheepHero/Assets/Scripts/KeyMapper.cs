using UnityEngine;
using System.Collections;

public class KeyMapper : MonoBehaviour {


	void Update() {
		//Chromatic keyboard
		// SheepScript sheep = GetComponent<SheepScript> ();

		/*
		if (Input.GetKeyDown (KeyCode.A)) {
			sheep.createSheep (true);
			SoundHelperScript.Instance.MakeDBah4Sound ();
		}
		
		else if (Input.GetKeyDown(KeyCode.W))
			SoundHelperScript.Instance.MakeDBah3Sound();

		else if (Input.GetKeyDown(KeyCode.S))
			SoundHelperScript.Instance.MakeDBah2Sound();

		else if (Input.GetKeyDown(KeyCode.E))
			SoundHelperScript.Instance.MakeDBah1Sound();

		else if (Input.GetKeyDown(KeyCode.D))
			SoundHelperScript.Instance.MakeBah0Sound();

		else if (Input.GetKeyDown(KeyCode.F))
			SoundHelperScript.Instance.MakeBah1Sound();

		else if (Input.GetKeyDown(KeyCode.T))
			SoundHelperScript.Instance.MakeBah2Sound();

		else if (Input.GetKeyDown(KeyCode.G))
			SoundHelperScript.Instance.MakeBah3Sound();

		else if (Input.GetKeyDown(KeyCode.Y))
			SoundHelperScript.Instance.MakeBah4Sound();

		else if (Input.GetKeyDown(KeyCode.H))
			SoundHelperScript.Instance.MakeBah5Sound();

		else if (Input.GetKeyDown(KeyCode.U))
			SoundHelperScript.Instance.MakeBah6Sound();

		else if (Input.GetKeyDown(KeyCode.J))
			SoundHelperScript.Instance.MakeBah7Sound();

		else if (Input.GetKeyDown(KeyCode.K))
			SoundHelperScript.Instance.MakeBah8Sound();
		*/

			
	}

}